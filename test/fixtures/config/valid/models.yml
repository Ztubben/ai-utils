version: 1
gating:
  - name: test
    run: make test
models:
  profiles:
    - key: claude-impl
      provider: claude
      model: claude-opus-5
    - key: codex-review
      provider: codex
      model: gpt-5-codex
  defaults:
    implementation: claude-impl
    review: codex-review
notify:
  github: octocat
